version: "3.9"
services:
  netsentinel:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NETSENTINEL_MODE=simulation
      - NETSENTINEL_API_PORT=5000
      - NETSENTINEL_LOG_LEVEL=INFO
      - NETSENTINEL_API_KEY=
    volumes:
      - ./netsentinel.db:/app/netsentinel.db
